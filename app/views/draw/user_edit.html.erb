<div align="center">
	<h1>お掃除抽選アプリ</h1><br>
	<h2>ユーザー情報編集画面</h2><br>
	<%= flash[:notice] %><br><br>
	<%= form_tag(draw_user_update_path(@user), :method => :put) do %>
		<table>
			<tr><td>名前：</td><td><%= text_field :user, :name %></td></tr>
		  	<tr><td>ユーザーID：</td><td><%= text_field :user, :user_id %></td></tr>
		  	<tr><td>パスワード：</td><td><%= password_field :user, :pass %></td></tr>
		  	<tr><td>メールアドレス：</td><td><%= text_field :user, :email %></td></tr>
		</table><br>
		<%= submit_tag "変更" %>
	<% end %><br>
	<p>※全項目入力必須</p>
	<p>※ユーザーID、パスワードは半角英数字で入力して下さい</p>
	<br><br>
	<%= button_tag type: 'button', onclick: "location.href='#{draw_user_index_path}'" do %>
		<%= "戻る"  %>
	<% end %>
</div>